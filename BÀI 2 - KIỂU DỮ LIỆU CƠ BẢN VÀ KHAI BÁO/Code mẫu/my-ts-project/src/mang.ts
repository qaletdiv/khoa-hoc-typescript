// Mảng số
let numbers: number[] = [1, 2, 3, 4, 5];
let scores: Array<number> = [100, 95, 80];

// Mảng chuỗi
let names: string[] = ["Alice", "Bob", "Charlie"];

// Mảng boolean
let statusFlags: boolean[] = [true, false, true];

// Mảng hỗn hợp (khi không biết trước kiểu hoặc muốn linh hoạt, 
// nhưng không khuyến khích)
let mixedArray: (string | number)[] = ["Hello", 123, "World", 456];